name: "CodeQL"

on:
  push:
    branches:
      - 'master'
      - 'release/*'
      - 'hotfix/*'
  pull_request:
    branches:
      - 'master'
      - 'release/*'
      - 'hotfix/*'
  schedule:
    # 03:21 UTC every Thursday - off-the-hour to avoid GitHub's scheduling crunch
    - cron: '21 3 * * 4'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

concurrency:
  group: codeql-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          # Covers app/, file_operations' Kotlin/Java side, portscanner, commons_compress_7z.
          # Needs an actual build (autobuild can't handle this multi-module + native setup
          # reliably), so we drive it manually with the same command the rest of CI uses.
          - language: java-kotlin
            build-mode: manual
          # The Rust side of file_operations. build-mode: none reads the source directly,
          # no Android cross-compile toolchain required.
          - language: rust
            build-mode: none

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up JDK 17
        if: matrix.language == 'java-kotlin'
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          distribution: "temurin"
          java-version: 17

      - name: Set up NDK
        if: matrix.language == 'java-kotlin'
        uses: nttld/setup-ndk@6a7e9c5494b25a37d1c4d2f13ddd0c44569ece22 # v1.5.0
        with:
          ndk-version: r28c
          link-to-sdk: true
          local-cache: true

      - name: Install Rust
        if: matrix.language == 'java-kotlin'
        uses: dtolnay/rust-toolchain@b3b07ba8b418998c39fb20f53e8b695cdcc8de1b # stable
        with:
          toolchain: stable

      - name: Cache Rust dependencies
        if: matrix.language == 'java-kotlin'
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            file_operations/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Setup Rust for Android
        if: matrix.language == 'java-kotlin'
        run: |
          cd file_operations
          chmod +x setup_rust_android.sh
          ./setup_rust_android.sh

      - name: Setup Gradle
        if: matrix.language == 'java-kotlin'
        uses: gradle/actions/setup-gradle@ac638b010cf58a27ee6c972d7336334ccaf61c96 # v4.4.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@ce28f5bb42b49b1da6f9e5ad90ce8b96d8c1b5cd # v3.29.0
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended

      - name: Build (debug variants only, matches the rest of CI)
        if: matrix.language == 'java-kotlin'
        run: ./gradlew assembledebug --stacktrace

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@ce28f5bb42b49b1da6f9e5ad90ce8b96d8c1b5cd # v3.29.0
        with:
          category: "/language:${{ matrix.language }}"
